<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TeamPay - Payment Required</title>
    <style>
        :root {
            --sidebar-dark-blue: #0a1f3a;
            --sidebar-dark-blue-hover: #0f2a4d;
            --golden-yellow: #e86029;
            --golden-yellow-light: #f7d7b3;
            --bg-white: #ffffff;
            --text-primary: #1e293b;
            --text-secondary: #64748b;
            --border-color: #e2e8f0;
            --red-warning: #dc2626;
            --red-light: #fee2e2;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--sidebar-dark-blue) 0%, var(--sidebar-dark-blue-hover) 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: var(--bg-white);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(10, 31, 58, 0.3);
            padding: 50px 40px;
            max-width: 650px;
            width: 100%;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, var(--red-warning) 0%, var(--golden-yellow) 100%);
        }

        .alert-icon {
            width: 90px;
            height: 90px;
            background: linear-gradient(135deg, var(--red-warning) 0%, #ef4444 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            animation: urgentPulse 1.5s infinite;
            border: 4px solid var(--red-light);
        }

        @keyframes urgentPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); box-shadow: 0 0 30px rgba(220, 38, 38, 0.5); }
        }

        .alert-icon::before {
            content: '⚠';
            font-size: 45px;
            color: white;
            font-weight: bold;
        }

        .status-badge {
            display: inline-block;
            background: var(--red-warning);
            color: white;
            padding: 12px 25px;
            border-radius: 25px;
            font-weight: 700;
            font-size: 1rem;
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 1px;
            animation: flash 2s infinite;
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .brand-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--sidebar-dark-blue);
            margin-bottom: 10px;
        }

        .brand-subtitle {
            font-size: 1.2rem;
            color: var(--text-secondary);
            margin-bottom: 30px;
            font-weight: 500;
        }

        .main-heading {
            font-size: 2.2rem;
            color: var(--red-warning);
            margin-bottom: 25px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .urgent-message {
            background: var(--red-light);
            border: 2px solid var(--red-warning);
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
            color: var(--text-primary);
        }

        .urgent-message h3 {
            color: var(--red-warning);
            font-size: 1.4rem;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .message {
            color: var(--text-primary);
            font-size: 1.1rem;
            line-height: 1.7;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .highlight {
            color: var(--red-warning);
            font-weight: 700;
            background: var(--red-light);
            padding: 2px 6px;
            border-radius: 4px;
        }

        .payment-details {
            background: linear-gradient(135deg, #f8fafc 0%, var(--golden-yellow-light) 100%);
            padding: 30px;
            border-radius: 15px;
            margin: 30px 0;
            border: 2px solid var(--golden-yellow);
        }

        .payment-details h3 {
            color: var(--sidebar-dark-blue);
            font-size: 1.4rem;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .payment-info {
            text-align: left;
            max-width: 400px;
            margin: 0 auto;
        }

        .payment-info p {
            margin-bottom: 10px;
            color: var(--text-primary);
            font-weight: 500;
        }

        .amount {
            font-size: 1.3rem;
            color: var(--red-warning);
            font-weight: 800;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            margin: 40px 0;
        }

        .btn {
            padding: 18px 35px;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            min-width: 180px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-urgent {
            background: linear-gradient(135deg, var(--red-warning) 0%, #ef4444 100%);
            color: white;
            box-shadow: 0 8px 25px rgba(220, 38, 38, 0.4);
            animation: buttonPulse 3s infinite;
        }

        @keyframes buttonPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .btn-urgent:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 15px 40px rgba(220, 38, 38, 0.6);
        }

        .btn-contact {
            background: transparent;
            color: var(--sidebar-dark-blue);
            border: 3px solid var(--sidebar-dark-blue);
        }

        .btn-contact:hover {
            background: var(--sidebar-dark-blue);
            color: white;
            transform: translateY(-3px);
        }

        .developer-section {
            background: var(--sidebar-dark-blue);
            color: white;
            padding: 30px;
            border-radius: 15px;
            margin-top: 30px;
        }

        .developer-title {
            font-size: 1.4rem;
            margin-bottom: 15px;
            font-weight: 700;
            color: var(--golden-yellow);
        }

        .contact-info {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            margin: 15px 0;
            flex-wrap: wrap;
        }

        .phone-number {
            color: var(--golden-yellow);
            font-weight: 800;
            font-size: 1.3rem;
            padding: 8px 15px;
            background: rgba(232, 96, 41, 0.2);
            border-radius: 25px;
            border: 2px solid var(--golden-yellow);
        }

        .consequences {
            background: #fef2f2;
            border: 2px solid var(--red-warning);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }

        .consequences h4 {
            color: var(--red-warning);
            font-size: 1.2rem;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .consequences ul {
            text-align: left;
            color: var(--text-primary);
            font-weight: 500;
        }

        .consequences li {
            margin-bottom: 8px;
            list-style-type: none;
            position: relative;
            padding-left: 25px;
        }

        .consequences li::before {
            content: '⚠';
            position: absolute;
            left: 0;
            color: var(--red-warning);
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px 20px;
                margin: 10px;
            }

            .brand-title {
                font-size: 2rem;
            }

            .main-heading {
                font-size: 1.8rem;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 280px;
            }

            .contact-info {
                flex-direction: column;
                gap: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="alert-icon"></div>
        
        <div class="status-badge">
            Payment Overdue
        </div>

        <h1 class="brand-title">TeamPay</h1>
        <p class="brand-subtitle">Payroll Management System</p>

        <h2 class="main-heading">Payment Required</h2>

        <div class="urgent-message">
            <h3>IMMEDIATE ACTION REQUIRED</h3>
            <p class="message">
                Your trial period has <span class="highlight">EXPIRED</span> and payment for the TeamPay system development is now <span class="highlight">OVERDUE</span>.
            </p>
        </div>

        <p class="message">
            This system was custom developed for your organization by <strong>Sethnnections</strong>. The agreed trial period has ended, and <span class="highlight">immediate payment is required</span> to continue system access and avoid service disruption.
        </p>

        <div class="consequences">
            <h4>Consequences of Non-Payment:</h4>
            <ul>
                <li>Immediate system access suspension</li>
                <li>Loss of payroll processing capabilities</li>
                <li>Potential data access restrictions</li>
                <li>Additional late payment fees may apply</li>
            </ul>
        </div>

        <div class="payment-details">
            <h3>Settlement Required</h3>
            <div class="payment-info">
                <p><strong>System:</strong> TeamPay Payroll Management</p>
                <p><strong>Developer:</strong> Sethnnections</p>
                <p><strong>Status:</strong> <span class="highlight">Payment Overdue</span></p>
                <p><strong>Action:</strong> <span class="amount">Pay immediately to restore access</span></p>
            </div>
        </div>

        <div class="action-buttons">
            <a href="#" class="btn btn-urgent">Make Payment Now</a>
            <a href="#" class="btn btn-contact">Contact Developer</a>
        </div>

        <div class="developer-section">
            <h3 class="developer-title">Contact Sethnnections</h3>
            <p>To settle your account and restore system access:</p>
            <div class="contact-info">
                <span class="phone-number">0992279994</span>
            </div>
            <p style="margin-top: 15px; font-size: 0.95rem; opacity: 0.9;">
                Professional development services require timely payment.<br>
                Contact us immediately to resolve this matter.
            </p>
        </div>
    </div>

    <!-- SweetAlert2 CDN -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const urgentBtn = document.querySelector('.btn-urgent');
            
            // Add extra attention to the payment button
            setInterval(() => {
                urgentBtn.style.transform = 'scale(1.02)';
                setTimeout(() => {
                    urgentBtn.style.transform = 'scale(1)';
                }, 200);
            }, 4000);

            // Button interactions
            const buttons = document.querySelectorAll('.btn');
            buttons.forEach(button => {
                button.addEventListener('click', function(e) {
                    e.preventDefault();
                    if (this.classList.contains('btn-urgent')) {
                        Swal.fire({
                            icon: 'warning',
                            title: 'Payment Required',
                            html: 'Please contact <b>Sethnnections</b> at <b>0992279994</b> to process your payment and restore system access.',
                            confirmButtonText: 'OK',
                            confirmButtonColor: '#e86029'
                        });
                    } else {
                        Swal.fire({
                            icon: 'info',
                            title: 'Contact Developer',
                            html: 'Contact <b>Sethnnections</b> at <b>0992279994</b> to discuss payment options.',
                            confirmButtonText: 'OK',
                            confirmButtonColor: '#0a1f3a'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>