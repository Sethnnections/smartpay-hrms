<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #0a1f3a 0%, #1e3a8a 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', sans-serif;
        }
        .error-container {
            text-align: center;
            color: white;
            max-width: 500px;
            padding: 2rem;
        }
        .error-number {
            font-size: 8rem;
            font-weight: 900;
            color: #FFD700;
            line-height: 1;
            margin-bottom: 1rem;
            text-shadow: 0 0 20px rgba(255, 215, 0, 0.3);
        }
        .error-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }
        .error-message {
            font-size: 1.1rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        .btn-home {
            background-color: #FFD700;
            border-color: #FFD700;
            color: #0a1f3a;
            font-weight: 600;
            padding: 12px 30px;
            border-radius: 8px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
            margin-right: 1rem;
        }
        .btn-home:hover {
            background-color: #e6c200;
            transform: translateY(-2px);
            color: #0a1f3a;
        }
        .btn-back {
            background: transparent;
            border: 2px solid #FFD700;
            color: #FFD700;
            font-weight: 600;
            padding: 10px 30px;
            border-radius: 8px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }
        .btn-back:hover {
            background-color: rgba(255, 215, 0, 0.1);
            color: #FFD700;
        }
        .floating-elements {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1;
        }
        .floating-elements i {
            position: absolute;
            color: rgba(255, 215, 0, 0.1);
            animation: float 6s ease-in-out infinite;
        }
        .floating-elements i:nth-child(1) { top: 20%; left: 10%; animation-delay: 0s; }
        .floating-elements i:nth-child(2) { top: 60%; left: 80%; animation-delay: 2s; }
        .floating-elements i:nth-child(3) { top: 40%; left: 20%; animation-delay: 4s; }
        .floating-elements i:nth-child(4) { top: 80%; left: 70%; animation-delay: 1s; }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body>
    <div class="floating-elements">
        <i class="fas fa-users fa-3x"></i>
        <i class="fas fa-chart-line fa-2x"></i>
        <i class="fas fa-building fa-4x"></i>
        <i class="fas fa-calculator fa-2x"></i>
    </div>
    
    <div class="error-container">
        <div class="error-number">404</div>
        <h1 class="error-title">Page Not Found</h1>
        <p class="error-message"><%= message %></p>
        <div class="mt-4">
            <a href="/admin" class="btn-home">
                <i class="fas fa-home me-2"></i>Go to Dashboard
            </a>
            <a href="javascript:history.back()" class="btn-back">
                <i class="fas fa-arrow-left me-2"></i>Go Back
            </a>
        </div>
    </div>
</body>
</html>